<%- include("header") -%>
<div class="box" id="heading">
  <h1><%= listTitle %></h1>
</div>
  <div class="box">
    <% newListItems.forEach(function(item){ %>
      <form action="/delete" method="post">
        <div class="item">
          <input type="checkbox" name="checkbox" value="<%= item._id %>"  onchange="this.form.submit()">
          <p name="heading" value="<%= listTitle %>"><%= item.name %></p>
        </div>
        <input type="hidden" name="listName" value="<%= listTitle %>"></input>
      </form>
    <%})%>
    <form action="/" method="post">
      <input type="text" name="newItem" autocomplete="off" placeholder="Add ">
      <button type="submit" name="addbtn" value="<%= listTitle %>">+</button>
    </form>
  </div>
    <footer class="foot" >@Copyright Kuruva Pavani</footer>

    <%- include("footer") -%>